- In Streamlit, applying CSS to structure cards requires targeting specific  combinations (e.g. ) because  containing unclosed divs will not wrap Streamlit native widgets correctly. This is the safest way to maintain standard widgets inside styled visual cards.
- In Streamlit, applying CSS to structure cards requires targeting specific `[data-testid]` combinations (e.g. `[data-testid="column"] > [data-testid="stVerticalBlock"] > [data-testid="stVerticalBlock"]`) because `st.markdown(..., unsafe_allow_html=True)` containing unclosed divs will not wrap Streamlit native widgets correctly. This is the safest way to maintain standard widgets inside styled visual cards.
